spring:
  application:
    name: configClient
  cloud:
    config:
#      discovery:
#        enabled: true
#        service-id: config
      profile: local
      label: master
      uri: http://localhost:8762